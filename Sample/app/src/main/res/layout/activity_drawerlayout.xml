<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/ly_activity_drawerlayout_main">

    <FrameLayout
        android:id="@+id/ly_activity_drawerlayout_frame"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#FFFFFF">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="向右滑动显示左边菜单"/>

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="显示左边菜单"
            android:layout_gravity="center_horizontal"
            android:onClick="show"
            android:id="@+id/btn_activity_drawerlayout_left"/>

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="显示右边菜单"
            android:layout_gravity="bottom|center_horizontal"
            android:onClick="show"
            android:id="@+id/btn_activity_drawerlayout_right"/>

        </FrameLayout>

    <ListView
        android:id="@+id/lv_activity_drawerlayout_list"
        android:layout_width="180dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@android:color/white"
        android:choiceMode="singleChoice"
        android:divider="@color/text_yellow"
        android:dividerHeight="1dp" />

    <ListView
        android:id="@+id/lv_activity_drawerlayout_list1"
        android:layout_width="180dp"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        android:background="@android:color/white"
        android:choiceMode="singleChoice"
        android:divider="@color/text_yellow"
        android:dividerHeight="1dp" />

</android.support.v4.widget.DrawerLayout>
